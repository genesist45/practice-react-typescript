export interface Name {
    name: string;
    age: number;
}

// Gihimo nato ang "Name" interface nga naay duha ka property:
// - name (string type)
// - age (number type)
// Gi-export nato ni para magamit sa lain file like sa PersonList.tsx
